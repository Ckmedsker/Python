import pyttsx3
engine = pyttsx3.init()
engine.say("Since IT students will be working in the Unix / Linux command line environment, it is useful to learn a least two common command-line text editors.")
engine.runAndWait()